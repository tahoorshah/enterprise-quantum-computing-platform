# Frontend (React)

Not built yet. We are deliberately building backend + quantum modules
first (Modules 1-5 logic), and wiring up the React dashboard once
those modules produce real data to display.

When we get here, this will be scaffolded with:
    npx create-react-app . 
or Vite:
    npm create vite@latest . -- --template react
